{
  "name": "fmms-demo",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://Kishanjvaghela.github.io/fmms-web-demo",
  "dependencies": {
    "dotenv": "^6.2.0",
    "fmms": "github:finterra/ppbm.js#master",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "sh -ac '. .env; react-scripts start'",
    "build": "sh -ac '. .env; react-scripts build'",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
